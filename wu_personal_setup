#!/bin/bash

yum -y install nano
ln -s pico nano

cd ~
git clone git@github.com:TheWeatherCompany/wxu-drupal.git

ln -s ~/wxu-drupal /dru

cd ~
wget https://dl.bintray.com/mitchellh/vagrant/vagrant_1.7.2_x86_64.rpm
rpm -Uvh vagrant_1.7.2_x86_64.rpm
rm vagrant_1.7.2_x86_64.rpm
